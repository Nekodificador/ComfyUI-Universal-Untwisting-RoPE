[build-system]
requires = ["flit_core>=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "comfyui-universal-untwisting-rope"
description = "Universal Untwisting RoPE: training-free style transfer for image DiT models via RoPE frequency control."
version = "3.0.0"
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = []

[project.urls]
Repository = "https://github.com/Nekodificador/ComfyUI-Universal-Untwisting-RoPE"

[tool.comfy]
PublisherId = "nekodificador"
DisplayName = "Universal Untwisting RoPE"
